package swordoffer;

public class ListNode {
    int val;
    ListNode next = null;

    ListNode(int val) {
        this.val = val;
    }
    static ListNode testList  = new ListNode(1).next =new ListNode(2).next = new ListNode(3).next = new ListNode(4);
}